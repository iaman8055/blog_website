PORT=3000
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/blog
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d

# Comma-separated list of allowed frontend origins in production, e.g. https://myblog.vercel.app
# Leave unset in local dev to allow all origins.
CORS_ORIGIN=
